require([
    'config'
], function (requireConfig) {

    /**
     * TODO LIST
     *
     * TODO
     * need fix. if user has friend audio - hidden plus
     */

    require.config(requireConfig);
    require(['app/app']);

});